// Constants shared across the package.

export const uiPort = 3000

// Mountpoint inside the subcontainer where bitcoind's volume is mounted via mountDependency.
export const btcMountpoint = '/mnt/bitcoind'
